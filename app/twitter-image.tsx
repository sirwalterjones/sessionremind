// Same card as the Open Graph image — re-exported so twitter:image is
// emitted explicitly instead of relying on scraper og:image fallbacks.
export { default, alt, size, contentType } from './opengraph-image'
